{
  "name": "fullstack-intern-task",
  "version": "1.0.0",
  "private": true,
  "description": "Mini SaaS Template Store — run client + server together",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "setup": "npm run setup --prefix server",
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
